# Example environment file for llmserver-rs
# Provide a strong password before starting docker-compose.pgml.yml
PGML_POSTGRES_PASSWORD="ChangeMe!UpdateImmediately#2025"
# Uncomment to automatically enable the PGML admin dashboard during development
# DATABASE_URL="postgresql://pgml_admin:${PGML_POSTGRES_PASSWORD}@localhost:6543/pgml"
# Set to "require" when connecting to Supabase or any TLS-only Postgres service
# PGML_TLS_MODE="disable"
# Protect the admin dashboard APIs with a bearer token (required in production)
# ADMIN_API_TOKEN="s3cure-Admin-Token-Replace-Me-Immediately"
